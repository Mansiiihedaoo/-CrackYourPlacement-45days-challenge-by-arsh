class Solution {
    public int maxProfit(int[] arr) {
        int maxPro = 0;
        int minPrice = Integer.MAX_VALUE;
   for (int i = 0; i < arr.length; i++) {
            minPrice = Math.min(minPrice, arr[i]); //7 1 
            maxPro = Math.max(maxPro, arr[i] - minPrice); //0 0 4 5
        }
        return maxPro;
        
    }
} 

TC : O(N)
SC : O(1)
